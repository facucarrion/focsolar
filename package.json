{
  "name": "focsolar-final",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "prettier": "^2.8.4",
    "standard": "^17.0.0",
    "vite": "^4.1.0"
  },
  "eslintConfig": {
    "extends": [
      "node_modules/standard/eslintrc.json"
    ]
  },
  "dependencies": {
    "gh-pages": "^5.0.0"
  }
}
